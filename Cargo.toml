[package]
name = "mega-download"
version = "0.1.0"
edition = "2021"

[dependencies]
megalib = { path = "../mega/megalib" }
tokio = { version = "1", features = ["full"] }
clap = { version = "4", features = ["derive"] }
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
